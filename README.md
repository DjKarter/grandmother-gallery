# Галлерея изображений

## Чтобы добавить новую картину:

1) Скопировать `name.webp` и `name.png` в` public/images/`
2) Добавить объект в `gallery-data.ts` — `width/height` опциональны, если лень измерять